import type { AlgorithmSmokeVector } from "../../core/types";

export const test: AlgorithmSmokeVector = { algorithmId: "source.microphone", inputKind: "none", expectedKind: "bits", fidelity: "exact" };
