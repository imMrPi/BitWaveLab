import type { AlgorithmSmokeVector } from "../../core/types";

export const test: AlgorithmSmokeVector = {
  algorithmId: "channel.cfo",
  inputKind: "samples",
  expectedKind: "samples",
  fidelity: "exact",
};
